export enum ClientMessageMethod {
  Email = "email",
  SMS = "sms",
}

export enum ClientMessageType {
  SendBookingsLink = "send-bookings-link",
  SendCalendarFile = "send-calendar-file",
  SendExtendedBookingsDate = "send-extended-bookings-date",
}
