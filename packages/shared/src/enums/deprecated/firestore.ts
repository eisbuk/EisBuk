export enum DeprecatedDuration {
  "1h" = "60",
  "1.5h" = "90",
  "2h" = "120",
}

export enum DeprecatedOrgSubCollection {
  Slots = "slots",
  SlotsByDay = "slotsByDay",
  Customers = "customers",
  Bookings = "bookings",
  BookingsByDay = "bookingsByDay",
}

export enum DeprecatedBookingSubCollection {
  SubscribedSlots = "subscribedSlots",
}

export enum DeprecatedSlotType {
  Ice = "ice",
  OffIceDancing = "off-ice-dancing",
  OffIceGym = "off-ice-gym",
}

export enum DeprecatedCategory {
  Adults = "adults",
}
