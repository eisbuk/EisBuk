import { DateTime } from "luxon";

/**
 * ISO date we're using across all tests
 */
export const testDate = "2021-03-01";
/**
 * DateTime representation of our test data
 */
export const testDateLuxon = DateTime.fromISO("2021-03-01");
