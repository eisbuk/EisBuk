export enum Routes {
  Login = "/login",
  Unauthorized = "/unautorized",
  Clienti = "/clienti/:secretKey",
  Debug = "/debug",
}

export enum PrivateRoutes {
  Root = "/",
  Atleti = "/atleti",
  Prenotazioni = "/prenotazioni",
}
